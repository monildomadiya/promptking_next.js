// Metadata is generated in page.jsx via generateMetadata
// This layout intentionally has no metadata to prevent duplicate title/H1 issues

export default function PromptLayout({ children }) {
  return children;
}
