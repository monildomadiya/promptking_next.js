// Metadata is generated in page.jsx via generateMetadata
// This layout intentionally has no metadata to prevent duplicate/conflicting metadata

export default function CategoryLayout({ children }) {
  return children;
}
